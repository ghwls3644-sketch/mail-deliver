# 텍스트 파일 줄바꿈 — Windows에서 CRLF, 저장소는 LF로 통일
* text=auto

# Python
*.py    text eol=lf

# 문서
*.md    text eol=lf
*.txt   text eol=lf

# 이미지 — 바이너리로 처리 (줄바꿈 변환 안 함)
*.png   binary
*.jpg   binary
*.jpeg  binary
*.webp  binary
